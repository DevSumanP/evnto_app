# Play Console uploads (staging/production) auth with this service account.
# ci_materialize_secrets.sh writes the file into android/, where fastlane runs.
json_key_file("fastlane-play-service-account.json")

# Staging and production share one Play app (com.evnto.app), different tracks.
package_name("com.evnto.app")